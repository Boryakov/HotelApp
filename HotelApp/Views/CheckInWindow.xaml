<Window x:Class="HotelApp.Views.CheckInWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HotelApp.Views"
        mc:Ignorable="d"
        Title="CheckInWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Guest Full Name:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#333333"/>
        <TextBox Grid.Row="1" Name="txtGuestName" Padding="5" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="Requested Comfort Class:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#333333"/>
        <ComboBox Grid.Row="3" Name="cmbRoomClass" Padding="5" Margin="0,0,0,20">
            <ComboBoxItem Content="Standard" IsSelected="True"/>
            <ComboBoxItem Content="JuniorSuite"/>
            <ComboBoxItem Content="Suite"/>
        </ComboBox>

        <UniformGrid Grid.Row="4" Columns="2" Rows="1">
            <Button Name="btnRegister" Content="Register Guest" Margin="0,0,5,0" Padding="6" Background="#107C41" Foreground="White" FontWeight="SemiBold"/>
            <Button Name="btnCancel" Content="Cancel" Margin="5,0,0,0" Padding="6" Background="#E1DFDD"/>
        </UniformGrid>
    </Grid>
</Window>
